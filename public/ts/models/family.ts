export class Family {
    familyId: number
    familyName: string

    constructor(
        familyId: number,
        familyName: string
    ) {
        this.familyId = familyId
        this.familyName = familyName
    }
}